import { PageHeader } from "@/components/layout/PageHeader";

export default function NewPropertyPage() {
  return (
    <PageHeader
      title="New property"
      description="Property form comes in the CRUD implementation slice."
    />
  );
}
